namespace NPTP.InputSystemWrapper.Editor.Generation
{
    /// <summary>
    /// The namespaces generated code is emitted into. These deliberately match the package's own namespaces
    /// so that moving generated code into its own assembly does not change any using directives in user code.
    /// </summary>
    internal static class GeneratedNamespaces
    {
        internal const string ROOT = "NPTP.InputSystemWrapper";
        internal const string ENUMS = ROOT + ".Enums";
        internal const string ACTIONS = ROOT + ".Actions";
        internal const string PLAYER = ROOT + ".Player";
    }
}
